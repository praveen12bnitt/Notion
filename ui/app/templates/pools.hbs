	<div class="container-fluid">
		<div class="row-fluid">
			<div class="span2">
				<table class='table'>
					<thead>
						<tr><th>{{#link-to 'pools.index'}}Pools{{/link-to}} 
							<i class="fa fa-plus-circle" {{action open}}></i>
						</th></tr>
					</thead>
					{{#each model}}
					<tr><td>
						{{#link-to 'pool' this}}{{name}}{{/link-to}}: <small class='muted'>{{this.applicationEntityTitle}}</small> {{#link-to 'studies' this}}Data{{/link-to}}
					</td></tr>						
					{{/each}}
				</table>
			</div>
			<div class="span9">
				{{outlet}}
			</div>
		</div>
		{{outlet modal}}
	</div>
